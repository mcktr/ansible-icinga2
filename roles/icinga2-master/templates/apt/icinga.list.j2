# {{ ansible_managed }}

deb https://packages.icinga.com/debian/ icinga-{{ ansible_distribution_release }} main
deb-src https://packages.icinga.com/debian/ icinga-{{ ansible_distribution_release }} main
